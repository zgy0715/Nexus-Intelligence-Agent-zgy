# NIA 运行步骤

## 首次安装（仅需执行一次）

### 1. 安装基础服务

| 服务 | 安装方式 | 验证命令 |
|------|----------|----------|
| Ollama | https://ollama.com/download 下载安装 | `ollama list` |
| Redis | `winget install Redis.Redis` | `redis-cli ping` → PONG |
| MongoDB | `winget install MongoDB.Server` | `mongosh --eval "db.runCommand({ping:1})"` → ok |
| Node.js 18+ | https://nodejs.org 下载安装 | `node --version` → v18+ |

### 2. 配置 Ollama 模型目录（如有自定义目录）

如果模型不在默认目录（`~/.ollama/models`），需要设置环境变量：

```powershell
# 例如模型在 D:\Ollama\Models
[System.Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "D:\Ollama\Models", "User")
```

然后重启 Ollama 服务。

### 3. 下载 Ollama 模型

```bash
ollama pull qwen2:7b-instruct
ollama pull qwen2.5-vl:3b
ollama pull nomic-embed-text
```

### 4. 安装项目依赖

```bash
# Python 依赖
cd "D:\Nexus Intelligence Agent\backend"
pip install -e .

# Node.js 依赖
cd "D:\Nexus Intelligence Agent\frontend"
npm install
```

### 5. 配置环境变量

```bash
cd "D:\Nexus Intelligence Agent\backend"
copy .env.example .env
```

默认配置无需修改。如需自定义，编辑 `.env` 文件：

```
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_LLM_MODEL=qwen2:7b-instruct
OLLAMA_VL_MODEL=qwen2.5-vl:3b
OLLAMA_EMBED_MODEL=nomic-embed-text
REDIS_URL=redis://localhost:6379/0
MONGO_URL=mongodb://localhost:27017
MONGO_DB=nia
```

### 6. 初始化数据库

```bash
cd "D:\Nexus Intelligence Agent\backend"
python -c "from nia.storage.database import DatabaseManager; db = DatabaseManager(); db.init_db(); print('OK')"
```

---

## 每次启动（需要 5 个终端）

### 终端 1：Ollama

```bash
ollama serve
```

保持运行，不要关闭。

### 终端 2：Redis

```bash
redis-server
```

保持运行，不要关闭。

### 终端 3：MongoDB

```bash
mongod --dbpath D:\MongoDB\data
```

保持运行，不要关闭。

### 终端 4：后端 API

```bash
cd "D:\Nexus Intelligence Agent\backend"
uvicorn nia.api.app:app --reload --host 0.0.0.0 --port 8000
```

验证：浏览器打开 http://localhost:8000/api/health ，看到 `{"status":"ok"}` 即成功。

保持运行，不要关闭。

### 终端 5：前端

```bash
cd "D:\Nexus Intelligence Agent\frontend"
npm run dev
```

浏览器打开 http://localhost:5173 ，看到 NIA 界面即成功。

保持运行，不要关闭。

---

## 使用说明

### 零配置抓取

1. 左侧导航点击「零配置抓取」
2. 输入目标 URL，如 `https://news.example.com`
3. 输入自然语言指令，如「提取新闻标题、发布时间和正文」
4. 勾选「JS渲染模式」可处理动态网页
5. 点击「开始抓取」，等待进度完成
6. 下方查看抓取结果

### 语义问答

1. 左侧导航点击「语义问答」
2. 在底部输入框输入问题，如「最近有哪些关于AI的新闻？」
3. AI 基于已爬取数据回答，附带来源引用
4. 点击「来源引用」可查看原文出处

### 数据浏览

1. 左侧导航点击「数据浏览」
2. 查看所有已爬取的数据表格
3. 顶部搜索栏输入关键词即时过滤
4. 底部分页器翻页浏览

### 任务监控

1. 左侧导航点击「任务监控」
2. 查看今日统计：总任务数、成功率、LLM调用次数、平均耗时
3. 查看队列状态：待抓取数量、死信队列
4. 域名分布柱状图展示 Top 10
5. 成功率低于 90% 会显示红色告警

### 系统设置

1. 左侧导航点击「系统设置」
2. 查看当前配置信息（Ollama/Redis/MongoDB 连接参数）
3. 查看三大服务连接状态（绿色=已连接，红色=断开）
4. 点击「刷新」重新检测连接状态

---

## 命令行爬虫

```bash
cd "D:\Nexus Intelligence Agent\backend"

# 普通模式
scrapy crawl smart -a start_urls="https://example.com" -a ai_instruction="提取标题和内容"

# JS 渲染模式（动态网页）
scrapy crawl smart -a start_urls="https://example.com" -a ai_instruction="提取产品信息" -a use_js=True

# 多个 URL
scrapy crawl smart -a start_urls="https://a.com,https://b.com" -a ai_instruction="提取文章列表"
```

---

## 功能演示

```bash
# 启动测试网页服务器
python -m demo.test_server

# 运行对比演示（传统爬虫 vs AI 爬虫）
python -m demo.demo_script
```

---

## 常见问题

### ModuleNotFoundError: No module named 'nia'

```bash
cd "D:\Nexus Intelligence Agent\backend"
pip install -e .
```

### FastAPI 未安装

```bash
pip install fastapi uvicorn
```

### 前端启动失败

```bash
node --version          # 确认 Node.js 已安装
rm -rf node_modules     # 清除缓存
npm install             # 重新安装
```

### API 请求跨域错误

确保后端 API 已启动：`uvicorn nia.api.app:app --reload --port 8000`

### Ollama 模型缺失

```bash
ollama pull qwen2:7b-instruct
ollama pull qwen2.5-vl:3b
ollama pull nomic-embed-text
```

### 端口被占用

```bash
netstat -ano | findstr :5173     # 前端
netstat -ano | findstr :8000     # 后端 API
netstat -ano | findstr :6379     # Redis
netstat -ano | findstr :27017    # MongoDB
```

### Redis 连接失败

```bash
redis-server         # 启动 Redis
redis-cli ping       # 验证，应返回 PONG
```

### MongoDB 连接失败

```bash
mongod --dbpath D:\MongoDB\data    # 启动 MongoDB
mongosh --eval "db.runCommand({ping:1})"   # 验证
```

### Ollama 模型不显示

```powershell
# 设置模型目录环境变量
[System.Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "D:\Ollama\Models", "User")

# 重启 Ollama
taskkill //F //IM ollama.exe
ollama serve
```

---

## 更新日志

### 2026-06-01 — 爬取流程修复

- 重写 crawl API，爬取现在真正执行（crawl4ai + AI 提取 + MongoDB + FAISS）
- 项目结构重组为 frontend/ + backend/
- 添加 Ollama 模型目录配置说明

### 2026-05-23 — 初始版本

- 完整的运行步骤文档
- 前后端分离启动指南
