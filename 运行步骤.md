# NIA 运行手册

> **v2.0 精简架构**：只需 **Redis + MongoDB** 两个本地服务。Embedding 走本地 fastembed（ONNX/CPU，无需 Ollama），向量库用本地 FAISS（无需 Docker/Qdrant），LLM 用 DeepSeek 云 API。
>
> 全文分四部分：① 首次安装 → ② 每次启动 → ③ 各模块使用 → ④ 排错与停止。

---

## 一、首次安装（仅需一次）

### 1. 准备外部条件

| 名称 | 说明 | 验证 |
|------|------|------|
| DeepSeek API Key | https://platform.deepseek.com/ 注册获取 | - |
| Redis 7+ | `winget install Redis.Redis` | `redis-cli ping` → `PONG` |
| MongoDB 7+ | `winget install MongoDB.Server` | `mongosh --eval "db.runCommand({ping:1})"` |
| Node.js 18+ | https://nodejs.org | `node --version` |
| Python 3.11+ | https://python.org | `python --version` |

> 不需要 Qdrant、Ollama、Docker。

### 2. 安装项目依赖

```bash
# 后端 Python 依赖（含 fastembed、faiss、crawl4ai、cssselect 等）
cd "D:/Nexus Intelligence Agent/backend"
pip install -r requirements.txt

# 前端 Node 依赖
cd "D:/Nexus Intelligence Agent/frontend"
npm install
```

### 3. 配置环境变量

```bash
cd "D:/Nexus Intelligence Agent/backend"
copy .env.example .env
```

编辑 `backend/.env`，**只需填入 DeepSeek API Key**，其余保持默认：

```env
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-你的key

EMBED_PROVIDER=fastembed        # 本地 ONNX，首次自动下载 bge-m3
EMBED_MODEL=BAAI/bge-m3
EMBED_DIM=1024
VECTOR_STORE=faiss              # 本地向量库

REDIS_URL=redis://localhost:6379/0
MONGO_URL=mongodb://localhost:27017
MONGO_DB=nia
```

### 4. 初始化数据库（建索引）

```bash
cd "D:/Nexus Intelligence Agent/backend"
python -c "from nia.storage.database import DatabaseManager; d=DatabaseManager(); d.init_db(); print('OK')"
```

### 5.（可选）JS 渲染内核

只有当你打算勾选页面里的「JS 渲染」时才需要：

```bash
python -m playwright install chromium
```

### 6.（可选）后端自检

```bash
cd "D:/Nexus Intelligence Agent/backend"
python verify.py        # 检查语法 / 模块导入 / 依赖
```

看到 `✅ 后端自检通过` 即可。

---

## 二、每次启动（4 个终端）

> 顺序：先起 Redis、MongoDB，再起后端、前端。

### 终端 1 — Redis
```bash
redis-server
```

### 终端 2 — MongoDB
```bash
mongod --dbpath D:\MongoDB\data
```

### 终端 3 — 后端 API
```bash
cd "D:/Nexus Intelligence Agent/backend"
uvicorn nia.api.app:app --port 8000
```
- 验证：浏览器打开 http://localhost:8000/api/health → `{"status":"ok"}`
- ⚠️ 用 JS 渲染时**不要加 `--reload`**（reloader 会反复重启浏览器）。

### 终端 4 — 前端
```bash
cd "D:/Nexus Intelligence Agent/frontend"
npm run dev
```

浏览器打开 **http://localhost:5173**。前端通过 Vite 代理把 `/api/*` 转发到后端 `:8000`，无需额外配置。

> 右上角 ☀️/🌙 可随时切换明暗主题（自动记忆）。

---

## 三、各模块使用说明

左侧导航共 6 个模块。

### 1. 🤖 自主 Agent（首页 `/`，核心功能）

给一个**目标**和**起点 URL**，Agent 会自主决定爬哪些页、跟随哪些链接、何时停止，并提取汇总成报告。

**操作步骤：**
1. **目标**：用自然语言描述要收集什么。例：
   - `收集 Anthropic 官网近期的产品发布与公告，整理成要点`
   - `梳理这个文档站点的核心功能模块和用法`
   - （也可点目标框下方的示例快速填入）
2. **种子 URL**：每行一个起点。例：`https://www.anthropic.com/news`
3. **预算与选项**：
   - **最大页数**：Agent 最多抓多少页（默认 20，越大越深入但越慢/越费 token）
   - **跟随深度**：从种子页向外跟随链接的层数
   - **JS 渲染**：动态网页（内容靠 JS 渲染）才勾选，需先装 chromium
4. 点 **「启动 Agent」**。

**运行时你会看到：**
- **实时活动**（左侧时间线，逐条动画出现）：
  | 类型 | 含义 |
  |------|------|
  | 🎯 开始 | 任务已启动 |
  | 🧠 思考 | Agent 的判断/计划（普通文字） |
  | 🔧 动作 | 调用的工具（`fetch_page` 抓页 / `crawl_links` 批量抓 / `extract_data` 结构化提取 / `record_finding` 记录 / `finish` 结束）|
  | 👁 观察 | 工具返回的结果摘要 |
  | 💡 发现 | 一条有价值的发现（同时进右侧栏）|
- **发现**（右侧栏）：累积的结构化发现卡片
- **进度条**：页面预算 / 步数预算 / 已访问 / 发现数
- 运行中可随时点 **「取消」**，Agent 会收尾并出报告

**结束后**：底部生成 **Markdown 情报报告**（带来源编号），右上角可一键复制。

> 停止条件：目标达成（Agent 自己调 `finish`）/ 预算耗尽 / 连续多步无新发现 / 你手动取消。
> 每次运行记录会存入 MongoDB 的 `agent_runs` 集合。

---

### 2. 🌐 批量 / 整站爬取（`/crawl`）

并发抓取多个 URL，或按深度跟随链接整站爬取；可选 AI 结构化提取。

**操作步骤：**
1. **起始 URL**：每行一个。
2. **提取指令**（可选）：
   - 留空 → 只抓取正文文本
   - 填写 → 对**每个页面**做 AI 结构化提取，例：`提取标题、发布日期、作者、正文要点`
3. **选项**：
   - **跟随深度**：`0` = 只抓你给的这些 URL；`>0` = 跟随页面里的链接继续爬（整站）
   - **最大页数**：整站爬取的上限
   - **仅同域**：只跟随与种子同域名的链接（防止爬出去）
   - **JS 渲染**：同上
4. 点 **「开始爬取」**。

**结果：**
- 顶部统计：已抓取页面数 / 提取发现数 / 总耗时
- 左侧「抓取进度」：每个页面实时出现（✅成功/❌失败 + 字符数）
- 右侧「提取结果」：填了提取指令时，展示每页的结构化 JSON
- 抓取到的页面会存入 `crawled_data`，可在「数据浏览」查看

> 与 Agent 的区别：这里是**你指定范围**的确定性批量爬取；Agent 是**自主决策**爬什么。

---

### 3. 💬 语义问答（`/qa`）

基于**已爬取的数据**做 RAG 语义检索问答。

**操作步骤：**
1. 先用 Agent 或批量爬取采集一些数据（否则无内容可答）。
2. 底部输入框提问，回车发送。首次进入有**示例问题**可一键提问。
3. 回答以 **Markdown** 渲染，命中数据时附**来源引用**（可点开原文链接）。
4. 右上角「清空」可清除聊天记录。

> 机制：先向量检索已爬数据，相关度足够则基于检索内容回答（带来源）；相关度不足则降级为普通 LLM 对话。

---

### 4. 📊 数据浏览（`/data`）

浏览所有已爬取的结构化数据。

- 顶部搜索框：按 **URL / 域名 / 标题** 实时过滤（输入 300ms 后自动搜索）
- 表格列：URL（可点开）/ 域名 / 标题 / 提取方法 / 创建时间（相对时间）
- 底部分页（每页 12 条）

---

### 5. 📈 任务监控（`/monitor`）

系统运行状态与统计图表（**每 30 秒自动刷新**，也可点「刷新」）。

- 指标卡：总任务数 / 成功率 / LLM 调用 / 平均耗时 / 队列待处理 / 失败任务
- 顶部横幅：成功率正常显示绿色，低于阈值显示红色告警
- **域名分布**柱状图（ECharts，自动适配暗色主题）

---

### 6. ⚙️ 系统设置（`/settings`）

- **服务状态**：实时检测 Redis / MongoDB / LLM Provider 连接（绿=已连接 / 红=未连接），点「刷新」重测
- **配置详情**：按 LLM / Embedding / 向量存储 / 基础设施 / 爬取·Agent / AI 参数 分组展示当前 `.env` 生效配置

---

## 四、排错与停止

### 常见问题

| 现象 | 解决 |
|------|------|
| `ModuleNotFoundError` | `pip install -r requirements.txt` |
| 首次问答/索引很慢 | fastembed 首次下载 bge-m3（约数百 MB），联网等待一次即可，之后离线缓存 |
| DeepSeek 连接失败 | 检查 `.env` 的 `DEEPSEEK_API_KEY`，确认能访问 `api.deepseek.com` |
| Redis/Mongo 未连接 | 确认终端 1/2 已启动；`redis-cli ping`、`mongosh` 自测 |
| 勾选 JS 渲染后报错 | `python -m playwright install chromium`；后端去掉 `--reload` |
| 前端能开但调用报错 | 确认后端 `:8000` 在跑、`/api/health` 正常 |
| 端口被占用 | `netstat -ano | findstr :8000`（后端）/ `:5173`（前端）/ `:6379` / `:27017` |
| 前端类型报错（开发时） | `npm run build` 看 tsc 报错信息 |

### 健康检查

```bash
curl http://localhost:8000/api/health      # 后端
python verify.py                            # 后端模块自检（backend 目录下）
```

### 停止服务

在各自终端按 `Ctrl + C`。前端/后端可随时重启；Redis、MongoDB 一般常驻。

---

## 端口一览

| 服务 | 端口 |
|------|------|
| 前端（Vite） | 5173 |
| 后端 API（FastAPI） | 8000 |
| Redis | 6379 |
| MongoDB | 27017 |

---

## 推荐体验路径

1. 启动四个服务，打开 http://localhost:5173
2. 首页**自主 Agent**：目标填「收集 Solidot 近期的科技资讯标题与核心要点」，种子填 `https://www.solidot.org`，最大页数 15、深度 1，启动，观察实时活动与最终报告
3. 切到**数据浏览**看抓到的页面
4. 到**语义问答**问「最近有哪些值得关注的科技动态？」
5. **任务监控**看统计图表，右上角 ☀️/🌙 试试明暗主题切换
